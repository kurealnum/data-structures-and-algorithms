class Helpers:
    def swap(arr: list, i: int, j: int) -> None:
        temp = arr[i]
        arr[i] = arr[j]
        arr[j] = temp

    def is_valid(matrix: list[list], r: int, c: int):
        pass
